setIcon(<draconicevolution:crafting_injector>)

//Set the title String
setTitle("First Things First")

//Set the description String
setDescription("Get some Basic Fusion Crafting Injectors, you'll need them")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("draconicevolution:draconium_block")

//Set the position
setPos(90,0)

//Hide the connection lines, so I can have a pretty shape

criteria = addCriteria("hasBasicInjector", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<draconicevolution:crafting_injector>)