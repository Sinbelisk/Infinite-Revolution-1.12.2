setIcon(<draconicevolution:chaotic_core>)

//Set the title String
setTitle("The First Steps + 3")

//Set the description String
setDescription("You hunted to extintion all the dragons from The End, was it worth it?")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("draconicevolution:chaos_shard")

//Set the position
setPos(334,0)

//Hide the connection lines, so I can have a pretty shape

criteria = addCriteria("hasChaosCore", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<draconicevolution:chaotic_core>)