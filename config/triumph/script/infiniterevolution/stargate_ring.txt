//Set the icon item
setIcon(<sgcraft:stargatering>)

//Set the title String
setTitle("To The Stars 1")

setFrameType("challenge")

//Set the description String
setDescription("Obtain Stargate Ring")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("infiniterevolution:bedrock_pickaxe")

//Set the position
setPos(-305,-272)

//Hide the connection lines, so I can have a pretty shape

criteria = addCriteria("hasStargateRing", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<sgcraft:stargatering>)