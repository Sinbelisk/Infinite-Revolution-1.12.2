//Set the icon item
setIcon(<avaritia:extreme_crafting_table>)

//Set the title String
setTitle("The origin of Madness")

//Set the description String
setDescription("Obtain a Extreme Crafting Table, now the real challenge begins")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("avaritia:obtain_matrix_block")

//Set the position
setPos(90,0)

//Hide the connection lines, so I can have a pretty shape


criteria = addCriteria("hasXtremeTable", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<avaritia:extreme_crafting_table>)