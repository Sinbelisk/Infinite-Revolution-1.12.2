//Set the icon item
setIcon(<avaritia:block_resource>)

//Set the title String
setTitle("Comically dense Block")

//Set the description String
setDescription("This block has a mass of 29.9 quadrillion metric tons... yeah.")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("avaritia:obtain_neutronium")

//Set the position
setPos(180,0)

//Hide the connection lines, so I can have a pretty shape


criteria = addCriteria("hasANeutroniumBlock", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<avaritia:block_resource>)