setIcon(<avaritiatweaks:enhancement_crystal>)

//Set the title String
setTitle("Cosmic Alchemy")

setFrameType("goal")

//Set the description String
setDescription("Get an Enhancement Crystal, the closest you'll ever get to a Philosopher's stone")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("avaritia:obtain_eternal_singularity")

//Set the position
setPos(270,30)

//Hide the connection lines, so I can have a pretty shape

criteria = addCriteria("hasEncCrystal", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<avaritiatweaks:enhancement_crystal>)